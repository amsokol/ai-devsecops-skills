# Quarantine (this product)

Approach: [`library/policy/quarantine.md`](library/policy/quarantine.md).

| Setting    | Value                                  |
| ---------- | -------------------------------------- |
| Quarantine | **N days** (e.g. `2 days` / 48 hours) |

Apply to every ecosystem listed in [`POLICY.md`](POLICY.md).

Optional: product-specific enforcement ties (e.g. package-manager cooldown
settings that must stay aligned with **N**).
