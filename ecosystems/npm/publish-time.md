# Ecosystem: npm (Node)

## Publish time (quarantine)

npm registry:

```text
https://registry.npmjs.org/<package>
```

Use `time[<version>]` (ISO timestamps). Scoped packages:
`https://registry.npmjs.org/@scope%2Fname`. Unknown → wait.
