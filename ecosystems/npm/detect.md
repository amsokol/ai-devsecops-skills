# Ecosystem: npm (Node)

## Detect

- `package.json` plus a lockfile: `package-lock.json`, `pnpm-lock.yaml`, or
  `yarn.lock`.
- Match the package manager to the lockfile (do not switch managers unless the
  product asks).
