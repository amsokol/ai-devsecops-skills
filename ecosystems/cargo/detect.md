# Ecosystem: Cargo (Rust)

## Detect

- Workspace or package `Cargo.toml`, `Cargo.lock`
- Optional: `rust-toolchain.toml` / `rust-toolchain` (toolchain pins — usually
  report-only unless product policy or an unlock comment allows bumps)
