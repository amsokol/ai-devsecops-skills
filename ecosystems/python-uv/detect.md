# Ecosystem: Python (uv)

## Detect

- `pyproject.toml` (`dependencies`, optional `[dependency-groups]`,
  `[build-system].requires`)
- Lock: `uv.lock` (preferred). Note `requirements*.txt` if present without uv.
