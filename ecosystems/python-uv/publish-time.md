# Ecosystem: Python (uv)

## Publish time (quarantine)

PyPI JSON:

```text
https://pypi.org/pypi/<package>/<version>/json
```

Use `upload_time_iso_8601` or `upload_time`. Unknown → treat as wait
([`quarantine.md`](../policy/quarantine.md)).
